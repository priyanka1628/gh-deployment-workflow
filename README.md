# gh-deployment-workflow

This repo demonstrates a GitHub Actions workflow that deploys a simple static site to **GitHub Pages** when `index.html` in `main` changes.

Visit: `https://<username>.github.io/gh-deployment-workflow/` after the first successful deployment.
